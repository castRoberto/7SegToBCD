#ifndef MACROS_H
#define MACROS_H

#define a bool(puerto & (1<<0))
#define b bool(puerto & (1<<1))
#define c bool(puerto & (1<<2))
#define d bool(puerto & (1<<3))
#define e bool(puerto & (1<<4))
#define f bool(puerto & (1<<5))
#define g bool(puerto & (1<<6))
#define h bool(puerto & (1<<7))

#endif // MACROS_H
