#ifndef CABECERA_H
#define CABECERA_H
#include <Arduino.h>

#define a bool(puerto & (1<<1))
#define b bool(puerto & (1<<2))
#define c bool(puerto & (1<<3))
#define d bool(puerto & (1<<4))
#define e bool(puerto & (1<<5))
#define f bool(puerto & (1<<6))
#define g bool(puerto & (1<<0))

#endif // CABECERA_H
